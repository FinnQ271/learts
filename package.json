{
  "name": "learts-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "node backend/server.js",
    "build:frontend": "npm run build --prefix frontend"
  }
}
